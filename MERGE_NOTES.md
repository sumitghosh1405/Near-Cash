# V16 merge notes

Built on the supplied Near Cash Cash Radar + Serverless Live Map build.

V16 keeps the existing routes and adds server-side Smart Matching, privacy-preserving nearby results, trust labels, notification preferences/deduplication, expiry handling, and a validated transaction state machine.

No mock users, fake transactions or fabricated listings were added.
